---
inherit: manifest_base.yml
host: fec-stage-web
services:
  - fec-creds-stage
env:
  FEC_CMS_URL: https://fec-stage-proxy.app.cloud.gov
  FEC_WEB_API_URL: https://fec-stage-api.app.cloud.gov
  FEC_WEB_DEBUG: true
  FEC_WEB_ENVIRONMENT: stage
  NEW_RELIC_APP_NAME: fec | web | stage
