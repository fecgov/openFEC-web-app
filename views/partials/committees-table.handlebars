{{#if resultsCount}}
    <div class="meta-box">
        <div id="results-count" class="results-count">
          <p>Results: {{resultsCount}} records</p>
            {{#if resultsRange}}
                <p>Viewing: {{currentResultsStart}} - {{currentResultsEnd}}</p>
            {{/if}}
        </div>
        {{#if paginationLinks}}
            <div class="pagination">
                {{#if prevURL}}
                    <a href="{{prevURL}}" id="prev" class="pagination__link"><i class="ti-arrow-left"></i> Previous {{perPage}}</a>
                {{/if}}
                {{#if nextURL}}
                    <a href="{{nextURL}}" id="next" class="pagination__link">Next {{perPage}} <i class="ti-arrow-right"></i></a>
                {{/if}}
            </div>
        {{/if}}
    </div><!--- .meta-box -->
{{/if}}

<table id="results">
    <thead>
        <tr>
            <th class="sortable-header">Name</th>
            <th class="sortable-header">Treasurer Name</th>
            <th class="sortable-header">State</th>
            <th class="sortable-header">Party</th>
            <th class="sortable-header">Type</th>
            <th class="sortable-header">Designation</th>
        </tr>
    </thead>
    <tbody>
        {{#each committees}}
            <tr>
                <td>{{name}}</td>
                <td>{{treasurer}}</td>
                <td>{{state}}</td>
                <td>{{party}}</td>
                <td>{{type}}</td>
                <td>{{designation}}</td>
            </tr>
        {{/each}}
    </tbody>
</table>
