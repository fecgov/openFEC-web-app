{{#if resultsCount}}
    <div id="results-count">Results: {{resultsCount}} records</div>
{{/if}}

{{#if resultsRange}}
    Viewing: {{currentResultsStart}} - {{currentResultsEnd}}
{{/if}}

{{#if paginationLinks}}
    <div class="pagination-links">
        {{#if prevURL}}
            <a href="{{prevURL}}" id="prev">Previous {{perPage}}</a>
        {{/if}}
        {{#if nextURL}}
            <a href="{{nextURL}}" id="next">Next {{perPage}}</a>
        {{/if}}
    </div>
{{/if}}

<table id="results">
    <thead>
        <tr>
            <th>Name</th>
            <th>Treasurer Name</th>
            <th>State</th>
            <th>Party</th>
            <th>Type</th>
            <th>Designation</th>
        </tr>

        {{#each committees}}
            <tr>
                <td>{{name}}</td>
                <td>{{treasurer}}</td>
                <td>{{state}}</td>
                <td>{{party}}</td>
                <td>{{type}}</td>
                <td>{{designation}}</td>
            </tr>
        {{/each}}
    </thead>
</table>
