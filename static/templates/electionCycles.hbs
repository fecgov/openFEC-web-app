<fieldset class="js-subcycle-select">
  <legend class="cycle-select__label">Time period:</legend>
  <div class="toggles">
    {{#each this}}
    <label>
      <input
          type="radio"
          name="cycle-toggle"
          value="{{max}}"
          data-election-full="{{electionFull}}"
          {{#if checked}}checked{{/if}}
        />
      <span class="button button--neutral">
        {{#if electionFull}}
        Full cycle:
        {{/if}}
        {{min}}–{{max}}
      </span>
    </label>
    {{/each}}
  </div>
</fieldset>
